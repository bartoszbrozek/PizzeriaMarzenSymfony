{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row row-centered">
            <div class="col-sm-4 col-centered"></div>
            <div class="col-sm-4 col-centered">
                <div class="item">
                    <div class="content">
                        {{ errorInfo }}
                        {{ app.session.get('id_product') }}

                        {% for cart_product in cart %}
                            {{ cart_product }}
                        {% endfor %}
                    </div>
                </div>
            </div>
            <div class="col-sm-4 col-centered"></div>
        </div>
        <div class="row">
            <div class="col-md-4"></div>
            <div class="col-md-4">
                <a class="btn btn-primary width-100" href="{{ path('cart_flush') }}" role="button">Wyczyść koszyk</a>
            </div>
            <div class="col-md-4"></div>
        </div>


    </div>
{% endblock %}